### Install OBS 2.5, upgrade to 2.9

* The contents of `be.tgz` are dumped in `/srv/obs`.
* `api_production.sql` gets imported into `api_production`.
* Any `.repo` added (`zypper ar`) in `0080-obs_update-25-29.sh`
